# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the vLLM-Omni project
# Test-only correctness baseline: disable the positive and both side graphs.
base_config: ../../../vllm_omni/deploy/vibevoice.yaml

stages:
  - stage_id: 0
    enforce_eager: true
    engine_extras:
      additional_config:
        vibevoice_runtime_config:
          diffusion_cuda_graph: false
          decode_cuda_graph: false
